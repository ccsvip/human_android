<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DUIX.Test">

        <!-- 主界面Activity -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 本地数字人配置Activity -->
        <activity
            android:name=".ui.activity.local.LocalMainActivity"
            android:exported="false">
        </activity>

        <!-- 本地数字人播放Activity -->
        <activity
            android:name=".ui.activity.local.LocalCallActivity"
            android:exported="false">
        </activity>

        <!-- 云端数字人Activity -->
        <activity
            android:name=".ui.activity.cloud.CloudMainActivity"
            android:exported="false">
        </activity>

        <!-- 欢迎页面Activity -->
        <activity
            android:name="ai.guiji.duix.sdk.welcome.WelcomeActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="false"
            android:hardwareAccelerated="true" />

    </application>

</manifest>